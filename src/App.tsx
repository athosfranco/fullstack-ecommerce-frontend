import { Button } from "@mui/material";
import Typography from "@mui/material/Typography";

function App() {
  return (
    <div>
      <Typography>Primeiro commit</Typography>
      <Button variant="contained">Hello World</Button>
    </div>
  );
}

export default App;
